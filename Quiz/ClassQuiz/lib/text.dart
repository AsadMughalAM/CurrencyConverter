// ignore: file_names
import 'package:flutter/material.dart';

class Text extends StatelessWidget {
  const Text(Color color, {super.key});
@override
  Widget build(BuildContext context) {
   return const Text(Color.fromARGB(255, 90, 70, 10)
   );
  }
}